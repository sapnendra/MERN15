let book = {
    title: "Books are the basis",
    author: "HGDKD",
    year: 2025,
    summary: (title, year) => {
        console.log(`${title},Published in ${year}`);
    }
}

const student = {name: "Sapnendra"};

// console.log(Object.keys(book));
// console.log(Object.values(book));
// Object.assign(student, book) 
console.log(student);